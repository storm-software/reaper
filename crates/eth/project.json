{
  "name": "reaper-eth",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "crates/eth/src",
  "targets": {
    "build": {
      "executor": "@monodon/rust:check",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/eth"
      },
      "configurations": {
        "production": {
          "profile": "maxperf"
        }
      }
    },
    "test": {
      "executor": "@monodon/rust:test",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/eth"
      },
      "configurations": {
        "production": {
          "profile": "maxperf"
        }
      }
    },
    "lint": {
      "executor": "@monodon/rust:lint",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/eth"
      },
      "configurations": {
        "production": {
          "profile": "maxperf"
        }
      }
    }
  },
  "tags": []
}
