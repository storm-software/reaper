{
  "name": "reaper-eth-order-telegram-bot",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/eth-order-telegram-bot/src",
  "targets": {
    "build-local": {
      "executor": "@monodon/rust:build",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/eth-order-telegram-bot"
      },
      "configurations": {
        "production": {
          "release": true
        }
      }
    },
    "build": {
      "cache": false,
      "executor": "nx:run-commands",
      "inputs": ["rust", "^production"],
      "outputs": ["{workspaceRoot}/apps/reaper-eth-order-telegram-bot/dist"],
      "dependsOn": ["clean", "^build"],
      "options": {
        "commands": [
          "cargo lambda build --release --output-format zip --package reaper-eth-order-telegram-bot --color always --verbose"
        ],
        "color": true
      }
    },
    "test": {
      "executor": "@monodon/rust:test",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/eth-order-telegram-bot"
      },
      "configurations": {
        "production": {
          "release": true
        }
      }
    },
    "lint": {
      "executor": "@monodon/rust:lint",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/eth-order-telegram-bot"
      }
    },
    "run": {
      "executor": "@monodon/rust:run",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/eth-order-telegram-bot"
      },
      "configurations": {
        "production": {
          "release": true
        }
      }
    }
  },
  "tags": []
}
